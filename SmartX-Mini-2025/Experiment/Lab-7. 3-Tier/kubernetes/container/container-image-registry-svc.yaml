apiVersion: v1
kind: Service
metadata:
  name: container-image-registry
  namespace: <your_namespace>
spec:
  selector:
    app: container-image-registry
  ports:
    - port: 80
      targetPort: 5000
  type: ClusterIP
